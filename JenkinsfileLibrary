#!groovy

@Library('common-library@master') _

def utils = new jenkins.utils.Utilities(this)

node() {
    stage('Clone and checkout') {
        checkout scm
    }

    stage('Build') {
        echo printInfo(name: env.BRANCH_NAME)
        utils.runBuild(env.BRANCH_NAME)
    }
}
